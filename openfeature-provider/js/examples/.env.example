# Confidence API Credentials
# Get these from your Confidence dashboard

# Client secret for flag resolution (required)
CONFIDENCE_CLIENT_SECRET=your-client-secret-here

# API client credentials for authentication (required)
CONFIDENCE_API_CLIENT_ID=your-api-client-id-here
CONFIDENCE_API_CLIENT_SECRET=your-api-client-secret-here

# Optional: Custom resolver URL (defaults to https://resolver.confidence.dev/v1)
# CONFIDENCE_RESOLVER_URL=https://resolver.confidence.dev/v1

# Optional: Number of resolves to run in benchmark (defaults to 500)
BENCHMARK_REPEATS=100
